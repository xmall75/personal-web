'use client'

import Link from "next/link";
import { useForm } from "react-hook-form";

export default function InternPage() {
    return (
        <main>
            <Link href="/intern/form">Form</Link>
        </main>
        );
    }
